    // let x="harsh" //harshjaiswal
    // let y="jaiswal"
    // let n=x.length//5
    // x=x.concat(y)
    // y=x.slice(0,n)
    // x=x.slice(n,x.length)
    // console.log(x," ",y)
    let x='shalini'
    let y='tiwari'
    x=x.concat(y)
    y=x.slice(0,x.length-y.length)
    x=x.slice(y.length)
    console.log(x,y)